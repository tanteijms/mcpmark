# Project Configuration

This directory contains the production configuration files for the analytics platform.

## Files
- `configs/` — All INI configuration files
- `migration_spec.md` — Specification for the INI→YAML migration

## Migration Plan
The team has decided to migrate from INI to YAML format for better tooling support.
See `migration_spec.md` for detailed migration rules and requirements.
